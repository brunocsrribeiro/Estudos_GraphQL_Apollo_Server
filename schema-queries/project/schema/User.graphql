type Profile {
  id: ID!
  name: String!
}

enum StatusUser {
  ACTIVE
  INACTIVE
  BLOCKED
}

type User {
  id: ID!
  name: String!
  email: String!
  age: Int
  salary: Float
  vip: Boolean
  profile: Profile
  status: StatusUser
}
